#include <stdio.h>
#include <stdlib.h>
//missao sera calcular os fretes


/*
int main()
{
    float peso_1 = 5.0, distancia_1=100.0, tarifa_base=0.50, frete_1;


    print("Pedido 1:\n");
    printf("peso: %.2lf kg\n", peso_1);
    printf("frete calculado: R$%.2lf\n", frete_1);

    frete_1 = (peso_1*2.00)+(distancia_1*tarifa_base);

    //==================================================

    float peso_2 = 12.5, distancia_2=350.0, tarifa_base=0.50, frete_2;


    print("Pedido 2:\n");
    printf("peso: %.2lf kg\n", peso_2);
    printf("frete calculado: R$%.2lf\n", frete_2);

    frete_2 = (peso_2*2.00)+(distancia_2*tarifa_base);

    //==================================================

    float peso_3 = 2.0, distancia_3=50.0, tarifa_base=0.50, frete_3;


    print("Pedido 3:\n");
    printf("peso: %.2lf kg\n", peso_3);
    printf("frete calculado: R$%.2lf\n", frete_3);

    frete_3 = (peso_3*2.00)+(distancia_3*tarifa_base);






    return 0;
}
*/


/*
float calcular_frete(float peso_kg, float distancia_km, float tarifa_base){
    float frete = (peso_kg*2.00)+(distancia_km*tarifa_base);
    return frete;
}

void exibir(int num_ped, float peso, float frete){
    printf("\n\nPedido %d:\n", num_ped);
    printf("peso: %.2lf kg\n", peso);
    printf("frete calculado: R$%.2lf\n", frete);
}


int main(){
    float peso_1 = 5.0, distancia_1=100.0;
    float peso_2 = 12.5, distancia_2=350.0;
    float peso_3 = 2.0, distancia_3=50.0;
    float tarifa_base = 0.50, frete;

    frete = calcular_frete(peso_1, distancia_1, tarifa_base);
    exibir(1, peso_1, frete);
    frete = calcular_frete(peso_2, distancia_2, tarifa_base);
    exibir(2, peso_2, frete);
    frete = calcular_frete(peso_3, distancia_3, tarifa_base);
    exibir(3, peso_3, frete);

    return 0;
}
*/



